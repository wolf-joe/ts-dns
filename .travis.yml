language: go

go:
  - 1.13.x

addons:
  apt:
    packages:
      - ipset

before_install:
  - go get -t -v ./...

script:
  - sudo bash -c "PATH=\$PATH:$PATH GOPATH=$GOPATH go test -race ./... -coverprofile=coverage.txt -covermode=atomic"

after_success:
  - bash <(curl -s https://codecov.io/bash)